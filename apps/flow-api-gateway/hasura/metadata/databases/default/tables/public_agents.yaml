table:
  name: agents
  schema: public
array_relationships:
  - name: accounts
    using:
      foreign_key_constraint_on:
        column: owner_agent_id
        table:
          name: accounts
          schema: public
