table:
  name: currencies
  schema: public
is_enum: true
array_relationships:
  - name: accounts
    using:
      foreign_key_constraint_on:
        column: currency
        table:
          name: accounts
          schema: public
